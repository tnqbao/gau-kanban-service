apiVersion: v1
kind: Service
metadata:
  name: gau-account-service
  namespace: bao-${DEPLOY_ENV}-env
spec:
  selector:
    app: gau-account-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
---