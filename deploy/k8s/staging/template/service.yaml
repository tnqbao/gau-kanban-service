apiVersion: v1
kind: Service
metadata:
  name: gau-kanban-service
  namespace: bao-${DEPLOY_ENV}-env
spec:
  selector:
    app: gau-kanban-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
---